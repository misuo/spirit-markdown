#ifndef MARKDOWN_PARSER_FORWARDS_HPP
#define MARKDOWN_PARSER_FORWARDS_HPP

namespace Markdown {

template <typename Iterator> struct StringParsers;
template <typename Iterator> struct HTMLParsers;
template <typename Iterator> struct InlineParsers;
template <typename Iterator> struct BlockParser;

}

#endif // PARSER_FORWARDS_HPP
